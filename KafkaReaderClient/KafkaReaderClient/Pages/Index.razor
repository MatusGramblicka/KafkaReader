@page "/"
@using System.Net.WebSockets

<PageTitle>Index</PageTitle>

@*<div>
    <div style="float: right">
        <div style="text-align:right; ">
            @if (_webSocket.State == WebSocketState.Open)
            {
                <p style="float:left;margin-right:5px;">Topic to listen:</p>
                <input style="float:left;margin-right:5px;" @bind="@_topic" />
                <button style="width: 100px;float:left;margin-right:5px;" class="btn btn-success" @onclick="@SendMessageAsync">Listen</button>
            }
            <button style="width: 100px;float:left;margin-right:5px;" class="btn btn-danger" @onclick="@ClearScreen">Clear</button>
            
        </div>
        <br/>
    </div>
</div>*@

@*<div class="col-md-8">
    <div class="row">
        <div class="col">
            <div style="float: left">
                    <div>
                        <h3>Kafka Topics:</h3>
                    </div>
                    @foreach (var kafkaTopic in KafkaTopicsList)
                    {
                        <div>
                            @kafkaTopic
                        </div>
                    } 
                </div>
        </div>
    </div>
</div>*@


@if (_webSocket.State == WebSocketState.Open)
{
    <div style="width: 100%; height: 500px">
        <textarea @ref=_textAreaRef style="width: 100%; height: 100%;" autofocus readonly>@_log</textarea>
    </div>
}